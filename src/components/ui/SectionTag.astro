---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<span
  class:list={[
    "text-xs w-fit text-muted-foreground uppercase tracking-wider text-center",
    className,
  ]}
>
  <slot />
</span>
