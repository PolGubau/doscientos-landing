---
import {
  type TranslationKey,
  getLangFromUrl,
  useTranslations,
} from "~/shared/i18n/utils";
import { getMetadata } from "~/shared/lib/constants";
import Button, { type ButtonProps } from "./Button.astro";

type Props = {
  labelKey?: TranslationKey;
  label?: string;
} & ButtonProps;

const { labelKey = "contact", label, ...buttonProps } = Astro.props;
const { getInTouch } = getMetadata();
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<a href={getInTouch.href}>
  <Button withArrow {...buttonProps}> {label ?? t(labelKey)} </Button>
</a>
