// @doscientos/configs@0.1.5 does not yet export the Astro convenience profile.
// That profile only extends this TypeScript policy, so this preserves the same
// rules until the package version is updated.
import { typescriptConfig } from '@doscientos/configs/oxlint/typescript'

export default typescriptConfig
